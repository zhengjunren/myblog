<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhengjunren.myblog.admin.mapper.MenuMapper">
  <resultMap id="BaseResultMap" type="cn.zhengjunren.myblog.admin.domain.Menu">
    <!--@mbg.generated-->
    <!--@Table menu-->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="i_frame" jdbcType="BIT" property="iFrame" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="component" jdbcType="VARCHAR" property="component" />
    <result column="parent_id" jdbcType="BIGINT" property="parentId" />
    <result column="sort" jdbcType="BIGINT" property="sort" />
    <result column="icon" jdbcType="VARCHAR" property="icon" />
    <result column="path" jdbcType="VARCHAR" property="path" />
    <result column="cache" jdbcType="BIT" property="cache" />
    <result column="hidden" jdbcType="BIT" property="hidden" />
    <result column="component_name" jdbcType="VARCHAR" property="componentName" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="permission" jdbcType="VARCHAR" property="permission" />
    <result column="type" jdbcType="INTEGER" property="type" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--@mbg.generated-->
    id, i_frame, `name`, component, parent_id, sort, icon, `path`, `cache`, hidden, component_name,
    create_time, permission, `type`
  </sql>

  <select id="findByRolesIdAndTypeIsNotInOrderBySortAsc" resultMap="BaseResultMap">
    SELECT
      m.id,
      m.`cache`,
      m.component,
      m.component_name,
      m.create_time,
      m.hidden,
      m.i_frame,
      m.icon,
      m.`name`,
      m.path,
      m.permission,
      m.parent_id,
      m.sort,
      m.type
    FROM
      menu AS m
        LEFT OUTER JOIN role_menu AS rm ON m.id = rm.menu_id
        LEFT OUTER JOIN role r ON rm.role_id = r.id
    WHERE
      r.id = #{id}
      AND ( m.type NOT IN ( #{type} ) )
    ORDER BY
      m.sort ASC
  </select>

  <select id="selectByRoleId" resultMap="BaseResultMap">
    SELECT m.*
    FROM menu AS m
    LEFT JOIN role_menu AS rm ON m.id = rm.menu_id
    LEFT JOIN role r on rm.role_id = r.id
    WHERE r.id = #{roleId}
  </select>


</mapper>
