<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhengjunren.myblog.system.mapper.RoleMapper">
  <resultMap id="BaseResultMap" type="cn.zhengjunren.myblog.system.domain.Role">
    <!--@mbg.generated-->
    <!--@Table role-->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--@mbg.generated-->
    id, `name`, description, create_time, update_time
  </sql>

  <select id="selectByUserId" resultMap="BaseResultMap">
    SELECT
      role.*
    FROM
      role,
      user,
      user_role
    WHERE
      user.id = user_role.user_id
      AND role.id = user_role.role_id
      AND user.id = #{userId}
  </select>

</mapper>
